namespace OperateR.Interfaces
{
    /// <summary>
    /// Represents a marker interface for notifications (events) that can be published through the mediator.
    /// Notifications do not return a result and can have multiple handlers.
    /// </summary>
    public interface INotification { }

}
